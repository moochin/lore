// Tile and viewport constants — kept in a separate module to avoid circular
// imports (config.ts imports scene classes which import these constants).

export const TILE_SIZE = 32;
export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;
